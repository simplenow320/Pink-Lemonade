{
  "system": "<GLOBAL_GUARDRAILS> Module: Grant Impact Reporting — produce a funder-ready report matching the uploaded template.",
  "context": {
    "template_sections": "{{template_sections}}",
    "reporting_period": { "start": "{{start_date}}", "end": "{{end_date}}" }
  },
  "task": {
    "instructions": [
      "Fill each template section completely using approved data.",
      "Add simple tables for metrics where relevant.",
      "Suggest one appropriate chart for the most important KPI if data allows and describe it clearly."
    ]
  },
  "validation": {
    "must_match": ["budget_math_correct","kpi_freshness_ok"]
  },
  "output_format": {
    "type": "markdown_with_headings_plus_tables",
    "end_with": "Source Notes"
  }
}
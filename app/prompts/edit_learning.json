{
  "system": "<GLOBAL_GUARDRAILS> Module: Edit Learning — learn phrasing and proof points from user edits; never overwrite core data.",
  "context": {
    "draft_type": "{{draft_type}}",
    "draft_id": "{{draft_id}}"
  },
  "task": {
    "instructions": [
      "Compare AI draft and final user text.",
      "Extract reusable phrases and preferred proof points.",
      "Return only items that are new additions or consistent patterns."
    ]
  },
  "output_format": {
    "type": "json",
    "shape": {
      "phrases_added": ["string"],
      "proof_points_added": ["string"],
      "notes": "string"
    },
    "end_with": "Source Notes"
  }
}